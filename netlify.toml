# Netlify configuration for the Jira Leaderboard

[build]
  publish = "public"
  functions = "netlify/functions"

# Proxy /api/* to Netlify Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
